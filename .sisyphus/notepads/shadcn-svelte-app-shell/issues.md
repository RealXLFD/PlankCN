2026-03-11: bun x shadcn-svelte@latest add --yes button sidebar separator also generated sidebar support files (sheet, tooltip, input, skeleton, and src/lib/hooks/is-mobile.svelte.ts); treat them as CLI-managed sidebar dependencies, not manual scope expansion.
2026-03-11: The Storybook add-on flow injected extra packages (`@chromatic-com/storybook`, `@storybook/addon-vitest`, `@storybook/addon-a11y`, `@storybook/addon-docs`) plus Vitest-specific `vite.config.ts` changes that were outside this plan item and needed manual cleanup.
2026-03-11: Storybook's init flow still surfaced a Playwright browser-install prompt even with `--no-download-check`; the add-on install completed, but browser binaries were intentionally not pulled during this infrastructure task.
2026-03-11: `lsp_diagnostics` for `src/app.css` still reports Biome parse errors on Tailwind v4 directives (`@custom-variant`, `@theme`, `@apply`) even though `bun run check` and `bun run build` succeed, so CSS-entrypoint diagnostics need to be interpreted as tooling false positives here.
2026-03-11: Task-5 verification exposed that this workspace's test runtime provides a broken `localStorage` object (missing `clear()` and `removeItem()`), so direct browser-storage assumptions in unit tests are unreliable unless the test installs its own Storage shim first.
2026-03-11: Task-5 verification also exposed that `import { mount } from 'svelte'` uses the server-side entry under Vitest here (`mount(...) is not available on the server`), so component-mount tests need a dedicated client renderer/setup if later tasks require DOM-level Svelte assertions.
2026-03-11: Task-5 Storybook verification exposed that calling `setMode(...)` from `mode-toggle.stories.svelte` during snippet/template evaluation throws `Svelte error: state_unsafe_mutation`; post-render hooks are required for Storybook-side `mode-watcher` synchronization.
2026-03-11: Task-5 Storybook click verification exposed that handing `document.startViewTransition` to the helper as a detached function causes `TypeError: Illegal invocation`; the component must pass a wrapper that calls the method on `document` itself.
2026-03-11: Task-6 verification showed that `render(AppShell, ...)` from `svelte/server` still trips a client-side `assign_nodes` error under Vitest for these local `.svelte` imports, so direct SSR component rendering is not a reliable assertion path here.
2026-03-11: The original task-6 mobile drawer story rendered the sidebar dialog without `data-testid="app-sidebar"` because `sidebar.svelte` spread root props onto `Sheet.Root` instead of the visible `Sheet.Content` in the mobile branch.
