2026-03-11: `bun x sv create . --template minimal --types ts --add tailwindcss="plugins:none" --install bun --no-dir-check` is the stable non-interactive scaffold command for this baseline; omitting the explicit Tailwind plugin option triggers an interactive prompt.
2026-03-11: For task-1 script normalization, placeholder commands must fail with a clear stderr message and exit code 1; a success-only `console.log(...)` placeholder is misleading because later QA can mistake it for configured tooling.
2026-03-11: shadcn-svelte v1.1.1  accepts non-interactive flags for , , and alias paths, but  still needs manual normalization in  to keep the TaskFlux-aligned  value explicit.
2026-03-11: shadcn-svelte v1.1.1 init accepts non-interactive flags for --base-color, --css, and alias paths, but style still needs manual normalization in components.json to keep the TaskFlux-aligned new-york value explicit.
2026-03-11: `bun x sv add 'vitest=usages:unit' playwright storybook --install bun --no-git-check --no-download-check` is the reliable non-interactive add-on command here; `sv add vitest playwright storybook` still pauses for the Vitest usage prompt.
2026-03-11: The generated Vitest baseline still needed an explicit `jsdom` devDependency for `vitest --passWithNoTests` to exit 0 when `vitest.config.ts` uses `environment: 'jsdom'`.
2026-03-11: Storybook manager still throws `SB_PREVIEW_API_0008 (EmptyIndexError)` in dev when the story index is empty even if `build-storybook` succeeds, so task-3 needs at least one real story file to make browser QA meaningful.
2026-03-11: `@storybook/addon-svelte-csf` can satisfy a children-based primitive with the smallest valid story shape: `defineMeta({ component })` plus `<Story name="...">child content</Story>`.
2026-03-11: Current shadcn-svelte dark-mode docs still require keeping the root CSS import in `src/routes/+layout.svelte` when mounting `<ModeWatcher />`; the docs example explicitly uses `import "../app.css"` before rendering the watcher and children.
2026-03-11: In this Bun/Vitest jsdom workspace, task-5 component tests are most stable when they avoid rendering the Svelte tree and instead unit-test a shared helper with an explicit in-test `localStorage` shim; the runtime's `localStorage` object is missing standard methods and the component-renderer path pulled in unrelated Svelte wrapper issues.
2026-03-11: In this Vitest setup, importing `mount` from `svelte` resolves through the server entry, so task-5 verification is more reliable when the view-transition path is exercised through an injected helper contract instead of trying to render the Svelte component directly.
2026-03-11: In `@storybook/addon-svelte-csf`, forcing Storybook args via `setMode(...)` inside snippet evaluation triggers Svelte's `state_unsafe_mutation`; an element action keeps the light/dark stories pinned while moving the `mode-watcher` mutation to post-render.
2026-03-11: Browser `document.startViewTransition` must be invoked with the real `document` receiver; passing the method around as a detached function from `mode-toggle.svelte` causes `TypeError: Illegal invocation` during the dark-story toggle click.
2026-03-11: For static Storybook browser QA in this workspace, opening stories through `index.html?path=/story/...` avoids the raw `iframe.html` favicon 404 noise and gives a clean console signal for story-runtime verification.
2026-03-11: The shadcn-svelte sidebar primitive only forwards `Sidebar.Root` rest props to the visible surface on desktop by default; to keep standardized hooks stable across breakpoints, the mobile `Sheet.Content` branch must receive the same passthrough props.
2026-03-11: Task 7 can keep `tests/e2e/smoke.test.ts` untouched by moving `playwright.config.ts` to `testDir: 'e2e'`; that lets `bun run test:e2e` execute the real shell contract at `e2e/app-shell.spec.ts` without widening scope.
2026-03-11: `bun run test:e2e` => `2 passed (12.6s)` with desktop coverage for `data-state`/`data-collapsible` sidebar transitions plus `<html>.dark` toggling, and it wrote `.sisyphus/evidence/task-7-shell-desktop.png` and `.sisyphus/evidence/task-7-shell-mobile.png`.
2026-03-11: `bun run dev -- --host 127.0.0.1 --port 4173` served `/` successfully (`READY 200 http://127.0.0.1:4173/`), and a live Playwright browser snapshot on that route exposed the expected topbar controls without any extra route content.
2026-03-11: After switching Playwright to the real `bun run dev -- --host 127.0.0.1 --port 4173` path, the fresh Vite server sometimes needs one or two no-op retries before shell control clicks mutate DOM state; `expect.poll(...)` around the real buttons keeps the assertions aligned with dev-runtime behavior instead of preview timing.
2026-03-11: Final task-7 evidence now clearly separates the default and dark desktop states: `.sisyphus/evidence/task-7-shell-desktop.png` captures the default shell, `.sisyphus/evidence/task-7-shell-desktop-dark.png` captures the dark-mode shell, and `.sisyphus/evidence/task-7-shell-mobile.png` remains the mobile drawer proof.
2026-03-11: `Sidebar.Trigger` accepts the same `Button` variant/size props as other shadcn buttons, but its internal `size-7` class still wins unless the caller also supplies a later size utility; `variant="outline" size="icon-sm" class="relative size-8"` is the smallest way to visually match `ModeToggle` without changing the collapse icon or trigger behavior.
