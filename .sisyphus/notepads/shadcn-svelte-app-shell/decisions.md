2026-03-11: Normalized the generated Tailwind entrypoint from `src/routes/layout.css` to `src/app.css` and updated `src/routes/+layout.svelte` to import it so the scaffold matches the plan's required file contract while keeping the default SvelteKit structure minimal.
2026-03-11: Added honest placeholder scripts for `test:e2e`, `storybook`, and `build-storybook`; they print that the tooling is not configured yet instead of pretending those later-task capabilities already exist.
2026-03-11: Tightened the placeholder scripts further so `test:e2e`, `storybook`, and `build-storybook` all exit with code 1 and clear stderr output until the real tooling lands in later tasks.
2026-03-11: Kept the shadcn-svelte CLI output structure exactly under  and , and normalized only  fields needed to satisfy the TaskFlux contract (, ) without relocating generated files.
2026-03-11: Kept the shadcn-svelte CLI output structure exactly under src/lib/components/ui/* and src/lib/utils.ts, and normalized only components.json fields needed to satisfy the TaskFlux contract (style and cssVariables) without relocating generated files.
2026-03-11: Restored `vite.config.ts` to the plain app plugin setup and moved unit-test settings into a dedicated `vitest.config.ts` so Storybook-specific testing add-ons do not own the main Vite config.
2026-03-11: Kept Storybook on `@storybook/sveltekit` with `@storybook/addon-svelte-csf` as the only addon, imported `../src/app.css` in `.storybook/preview.ts`, and used a toolbar-driven `document.documentElement` class toggle for `light` and `dark` instead of any SvelteKit route API.
2026-03-11: Replaced the generated demo Playwright spec with a single skipped smoke spec at `tests/e2e/smoke.test.ts` so `bun run test:e2e -- --list` validates the configured project without carrying demo assertions into later shell tasks.
2026-03-11: Added one real Storybook story at `src/lib/components/ui/button/Button.stories.svelte` for the existing shadcn button primitive instead of reintroducing demo/sample content, which keeps Storybook usable in-browser without expanding scope.
2026-03-11: Kept `src/app.css` on the zinc-only shadcn variable set and sidebar tokens, and ported only the TaskFlux shell base-layer improvements (`color-scheme`, scrollbar treatment, and body colors) instead of copying the alternate `neutral`/`stone`/`slate`/`gray` palette matrices.
2026-03-11: Mounted `ModeWatcher` directly in `src/routes/+layout.svelte` ahead of `{@render children?.()}` while preserving the existing favicon handling so the dark-mode root path stays minimal and aligned with the current shadcn-svelte docs.
2026-03-11: For task 5, kept `mode-watcher` as the only authority for mode changes but switched `ModeToggle` itself to a native `<button>` styled with `buttonVariants(...)` instead of the shadcn `Button` component so the toggle stays visually aligned while avoiding snippet-wrapper test/runtime issues in this workspace.
2026-03-11: Extracted the task-5 branch-selection logic into `src/lib/components/app/mode-toggle.ts` so the component still owns the real `document.startViewTransition` reveal path while the unit suite can deterministically verify animated/immediate fallback behavior plus storage/class side effects without rendering the full Svelte tree.
2026-03-11: Kept task-5 on the extracted `mode-toggle.ts` helper for unit verification, made the Svelte component import explicit via `./mode-toggle.ts`, and synchronized the Storybook story's forced state with both `document.documentElement` and `mode-watcher` so the light/dark stories stay pinned without adding any custom theme store.
2026-03-11: Repaired the task-5 Storybook runtime failure by moving the forced `setMode(...)` call in `src/lib/components/app/mode-toggle.stories.svelte` out of snippet evaluation and into a Svelte action, then inlined the template per story so the light/dark routes stay real, pinned, and free of `state_unsafe_mutation`.
2026-03-11: Repaired the remaining task-5 runtime bug by wrapping `document.startViewTransition` in `src/lib/components/app/mode-toggle.svelte` before passing it into `toggleModeWithTransition`, preserving the radial animation path while keeping the browser method bound to `document`.
2026-03-11: For task 6, kept `AppShell` purely presentational by making it own only `Sidebar.Provider` + `Sidebar.Inset` composition, plumbing a simple `open` prop for desktop state and a `mobileOpen` prop through `AppSidebar` so Storybook can express desktop expanded, desktop collapsed, and mobile drawer-open states without any route coupling.
2026-03-11: Because `svelte/server` rendering of the new shell component was unstable under Vitest here, task-6 unit coverage uses deterministic file-contract assertions against the new `.svelte` files instead of `mount(...)` or brittle client rendering.
2026-03-11: Repaired the task-6 mobile selector gap by moving `Sidebar.Root` rest-prop passthrough onto the mobile `Sheet.Content` surface in `src/lib/components/ui/sidebar/sidebar.svelte`, which keeps `data-testid="app-sidebar"` attached to the visible drawer without changing the shell API or story names.
2026-03-11: Replaced the task-6 source-string test with a node-environment `svelte/server` rendered-output test in `src/lib/components/app/app-shell.test.ts`; asserting the emitted HTML keeps the suite deterministic while satisfying the rendering-based acceptance bar.
2026-03-11: For task 7, kept `src/routes/+page.svelte` as a pure route wrapper that only imports and renders `AppShell`, preserving the shell's presentational ownership and avoiding any demo copy or route-side layout logic.
2026-03-11: Left `.storybook/preview.ts` untouched because it already imports `../src/app.css` and synchronizes `document.documentElement` theme classes/color-scheme, so Storybook and the app route stay on the same CSS/theming path without extra wiring.
2026-03-11: Final-wave remediation kept task 7 scoped to Playwright only: `playwright.config.ts` now boots `bun run dev -- --host 127.0.0.1 --port 4173` instead of `build && preview`, `e2e/app-shell.spec.ts` now writes separate default/dark desktop evidence while preserving the required mobile artifact, and `tests/e2e/smoke.test.ts` was deleted as stale scaffold content.
2026-03-11: For the collapse-button restyle, updated only `src/lib/components/app/app-topbar.svelte` and left `src/lib/components/app/app-shell.stories.svelte` unchanged because the existing `AppShell` stories already render `AppTopbar` through `AppShell`; changing the shared component preserves routed-app and Storybook parity without story-only divergence.
