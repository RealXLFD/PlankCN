2026-03-11 task-5 final verification

$ bun run test:unit
$ vitest
RUN v4.0.18 /Users/realxlfd/Desktop/Todo/shadcn-startup
✓ src/lib/components/app/mode-toggle.test.ts (3 tests) 6ms
Test Files 1 passed (1)
Tests 3 passed (3)
Duration 375ms

$ bun run build-storybook
$ storybook build
Storybook build completed successfully
Output directory: /Users/realxlfd/Desktop/Todo/shadcn-startup/storybook-static
Notes:
- Storybook still warns that `src/**/*.mdx` matched no files.
- Vite still warns about the large `iframe` chunk size.
- Both warnings are non-blocking; the build exits successfully.
2026-03-11 task-5 retry:
- Updated `src/lib/components/app/mode-toggle.svelte` to keep the strict `light <-> dark` toggle on `mode-watcher` and make the helper import explicit via `./mode-toggle.ts`.
- Updated `src/lib/components/app/mode-toggle.test.ts` to cover: animated transition path, missing `startViewTransition` immediate fallback, and reduced-motion immediate fallback without relying on broken workspace `localStorage` methods.
- Updated `src/lib/components/app/mode-toggle.stories.svelte` so the Light and Dark stories pin both `document.documentElement` and `mode-watcher` to the forced mode.
- Verification: `bun run test:unit` -> PASS (1 file, 3 tests).
- Verification: `bun run build-storybook` -> PASS (completed successfully; existing large-chunk warning remains non-blocking).
2026-03-11 story runtime fix:
- Repaired `src/lib/components/app/mode-toggle.stories.svelte` by replacing render-time `syncMode(...)` evaluation with a `use:forceMode` action that applies `setMode(...)` after mount/update.
- Kept both stories real and pinned with per-story inline templates: `Light` forces `light`, `Dark` forces `dark`.
- Verification: `bun run build-storybook` -> PASS after the story-only fix.
- Browser verification: `http://127.0.0.1:6006/?path=/story/app-modetoggle--light` and `http://127.0.0.1:6006/?path=/story/app-modetoggle--dark` both rendered with 0 console errors in Playwright.
2026-03-11 Illegal invocation fix:
- Updated `src/lib/components/app/mode-toggle.svelte` to pass a wrapped `startViewTransition` callback that invokes `document.startViewTransition(...)` with the correct `document` receiver.
- Kept the same task-5 transition flow, including the radial reveal animation path and strict `light <-> dark` toggle behavior.
- Verification: `bun run test:unit` -> PASS (1 file, 3 tests).
- Verification: `bun run build-storybook` -> PASS.
- Browser verification: clicked `http://127.0.0.1:6006/?path=/story/app-modetoggle--dark` toggle in Playwright; console remained at 0 errors, so the previous `TypeError: Illegal invocation` no longer occurs.
