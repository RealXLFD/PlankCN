2026-03-11 Task 6 shell emptiness and contract evidence

Files added:
- src/lib/components/app/app-shell.test.ts

Contract assertions implemented in `src/lib/components/app/app-shell.test.ts`:
- `app-shell.svelte` contains:
  - `data-testid="app-shell"`
  - `Sidebar.Provider`
  - `AppSidebar mobileOpen={mobileOpen}`
  - `Sidebar.Inset`
  - `AppTopbar`
  - `MainView`
- `app-topbar.svelte` contains:
  - `data-testid="app-topbar"`
  - `Sidebar.Trigger` with `aria-label="Toggle sidebar"`
  - vertical `Separator`
  - flexible spacer `<div class="flex-1"></div>`
  - `ModeToggle`
- `mode-toggle.svelte` contains `aria-label="Toggle dark mode"`
- `app-sidebar.svelte` contains an empty `Sidebar.Root` shell with `data-testid="app-sidebar"` and `collapsible="icon"`
- `main-view.svelte` contains an empty `<div data-testid="main-view" class="flex-1 overflow-auto"></div>`
- negative assertions ensure no `TaskFlux`, no `Sidebar.Header`, no `Sidebar.Content`, and no `placeholder` text in the empty containers

Static verification:
- `lsp_diagnostics` on all six modified source/test/story files: no diagnostics
- `$app/` grep across new shell components/stories/tests: no matches

Unit verification:
- Command: `bun run test:unit`
- Result: exit 0
- Passing files:
  - `src/lib/components/app/app-shell.test.ts`
  - `src/lib/components/app/mode-toggle.test.ts`

2026-03-11 follow-up rendered-output test evidence

Test rewrite summary:
- Replaced the previous `readFileSync(...)` source-string test with a rendered-output test using `svelte/server`
- `src/lib/components/app/app-shell.test.ts` now runs in a node-only Vitest environment and asserts the emitted HTML from `render(AppShell, { props })`

Commands run:
- `bun run test:unit`

Rendered-output assertions now cover:
- required hooks in emitted HTML:
  - `data-testid="app-shell"`
  - `data-testid="app-sidebar"`
  - `data-testid="app-topbar"`
  - `data-testid="main-view"`
  - `aria-label="Toggle sidebar"`
  - `aria-label="Toggle dark mode"`
- empty shell expectations in emitted HTML:
  - collapsed render includes `data-state="collapsed"`
  - no `TaskFlux`
  - no `placeholder`
  - no extra sidebar content wrappers or filler copy

Additional static checks:
- `lsp_diagnostics` on `src/lib/components/ui/sidebar/sidebar.svelte`: no diagnostics
- `lsp_diagnostics` on `src/lib/components/app/app-shell.test.ts`: no diagnostics
- `$app/` grep across task-6 shell components/stories/tests: no matches
